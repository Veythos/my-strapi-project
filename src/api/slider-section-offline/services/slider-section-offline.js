'use strict';

/**
 * slider-section-offline service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::slider-section-offline.slider-section-offline');
