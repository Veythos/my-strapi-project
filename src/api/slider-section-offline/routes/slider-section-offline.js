'use strict';

/**
 * slider-section-offline router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::slider-section-offline.slider-section-offline');
