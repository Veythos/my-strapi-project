'use strict';

/**
 * slider-section-offline controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::slider-section-offline.slider-section-offline');
